{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/tiny_renderer_rust.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "console": "externalTerminal"
        }
    ]
}
